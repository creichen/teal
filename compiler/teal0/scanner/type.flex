// names for built-in types, TEAL-0 only
"int"         { return sym(Terminals.INT); }
"string"      { return sym(Terminals.STRING); }
"any"         { return sym(Terminals.ANY); }
"array"       { return sym(Terminals.ARRAY); }
