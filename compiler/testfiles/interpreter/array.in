// IN: 11
// OUT: 55
fun main(n : int) = {
    var a : array[int] := new array[int](n);
    var i := 0;

    while (i < n) {
    	  a[i] := i;
    	  i := i + 1;
    }

    var s := 0;

    i := 0;
    while (i < n) {
    	  s :=  s + a[i];
	  i := i + 1;
    }

    return s;
}