// IN:NONE
// EXCEPTION: lang.ir.InterpreterException
// A test file where we insert the an element of the wrong type into an array.
fun main() = {
    var a : array[int] := new array[int](3);
    a[0] := 1;
    a[1] := "Oh no!";
    a[2] := 2;
    return a[1];
}
