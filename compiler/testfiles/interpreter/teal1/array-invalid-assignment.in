// IN: 0
// EXCEPTION: lang.ir.QualifierException
// A test file where we insert the an element of the wrong type into an array.
fun main(x) = {
	var a : array[nonnull int] := [1, 2, 3];
	if x == 0 {
		a[1] := null;
	}
	return a;
}
